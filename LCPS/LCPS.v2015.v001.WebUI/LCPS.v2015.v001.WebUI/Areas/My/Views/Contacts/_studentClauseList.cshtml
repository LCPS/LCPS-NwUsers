@using Anvil.v2015.v001.Domain.Entities.DynamicFilters;

@using LCPS.v2015.v001.NwUsers.Filters;

@model DynamicStudentClause

<table class="table table-hover table-striped">
    <tr>
        <th>Clause</th>
        <th></th>
    </tr>
    @foreach (DynamicQueryClauseField c in Model)
    {
        <tr>
            <td>
                @c.ToString()
            </td>
            <td>
                <a href="@Url.Action("DeleteStudentClause", "Contacts", new { area = "My" })">Delete</a>
            </td>
        </tr>
    }
</table> 
