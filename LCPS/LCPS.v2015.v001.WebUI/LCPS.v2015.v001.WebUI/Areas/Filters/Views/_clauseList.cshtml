@using Anvil.v2015.v001.Domain.Entities.DynamicFilters;

@model IEnumerable<IDynamicFilterClause>

<table class="table table-hover table-striped">
    <tr>
        <th>
            Clause
        </th>
        <th>&nbsp;</th>
    </tr>
    <tr>
        @foreach (IDynamicFilterClause c in Model)
        {
            <td>
                @c.ToFriendlyString()
            </td>
            <td>

            </td>
        }
    </tr>
</table>
